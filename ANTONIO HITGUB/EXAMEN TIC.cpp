// Examen
int numero //variablle que guarda un nºentero
#inclue <iostream> //incluye números

#include <fstream> //incluye ficheros

#include <string> //incluye palabras

float numero //varable que guarda un nº decimal

string palabra //variable que guarda una palabra

int notas[30] vector para guardar 30 posiciones de memoria para números

char nombre[10] vector para guardar 10 posiciones de memoriapara letras

int main() // agrupa parte del programa
{
}

ifstream fichero ("prueba.txt");  //leer fichero

ofstream //crear y meter datos

while (! fichero.eof()) //si no ha acabado de leer el fichero continua leyendo
{
	getline(fichero,linea) //lee una linea del fichero
	cout << linea << endl; //con esto aparece en pantalla la linea del fichero
}
	
char datos[3];{} //guarda 3 palabras separadas por comas 

	
